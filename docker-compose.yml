services:
  database:
    build:
      context: ./mongodb
    container_name: mongo_database
    ports:
      - "27017:27017"

  backend:
    build:
      context: ./app
    ports:
      - "5000:5000"